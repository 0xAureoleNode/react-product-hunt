const ProductList = () => {
  return (
    <div className="ui unstackable items">
      hello react, I'm basic functional component
    </div>
  );
};

ReactDOM.render(<ProductList />, document.getElementById('content'));
